language: c
install: wget https://raw.githubusercontent.com/ocaml/ocaml-travisci-skeleton/master/.travis-opam.sh
script: bash -ex .travis-opam.sh
sudo: required
env:
    - OCAML_VERSION=4.01 
      EXTRA_DEPS="mirage"
      POST_INSTALL_HOOK="cd examples && mirage configure --xen && make && mirage clean"
