language: c
install: wget https://raw.githubusercontent.com/ocaml/ocaml-ci-scripts/master/.travis-docker.sh
script: bash -ex .travis-docker.sh
services:
- docker
env:
  global:
  - PINS="mirage-bootvar-xen:."
  - DISTRO="debian-stable"
  matrix:
  - PACKAGE="mirage-bootvar-xen" OCAML_VERSION="4.04.2"
  - PACKAGE="mirage-bootvar-xen" OCAML_VERSION="4.05.0"
  - PACKAGE="mirage-bootvar-xen" OCAML_VERSION="4.06.0"
